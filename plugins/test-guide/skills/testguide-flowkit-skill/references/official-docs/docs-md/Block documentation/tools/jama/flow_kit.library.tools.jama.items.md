[![logo](../../../res/flow.svg)](../../../index.html "flow.kit documentation") flow.kit documentation

[ Home ](../../../index.md)

Help contents

Help contents

- [ Introduction ](../../../introduction.md)
- [ Setup IDE ](../../../setup_ide.md)
- [ How to build a flow definition ](../../../how_to_build_a_flow_definition.md)
- [ Validate, execute and visualize your flow definition ](../../../validate_execute_visualize.md)
- [ Python environment ](../../../python_environment.md)
- [ Proxy configuration ](../../../proxy.md)
- [ Retries configuration ](../../../retries.md)

Block documentation

Block documentation

[ control_flow ](../../flow_kit.library.control_flow.md)

[ ecu_test ](../../flow_kit.library.ecu_test.md)

[ environment ](../../flow_kit.library.environment.md)

[ file_operation ](../../flow_kit.library.file_operation.md)

[ test_guide ](../../test_guide/index.md)

test_guide

- [ artifacts ](../../test_guide/flow_kit.library.test_guide.artifacts.md)
- [ execution ](../../test_guide/flow_kit.library.test_guide.execution.md)
- [ flow ](../../test_guide/flow_kit.library.test_guide.flow.md)
- [ platform ](../../test_guide/flow_kit.library.test_guide.platform.md)
- [ q_gates ](../../test_guide/flow_kit.library.test_guide.q_gates.md)
- [ releases ](../../test_guide/flow_kit.library.test_guide.releases.md)
- [ report_mgmt ](../../test_guide/flow_kit.library.test_guide.report_mgmt.md)
- [ reporting ](../../test_guide/flow_kit.library.test_guide.reporting.md)
- [ test_infrastructure ](../../test_guide/flow_kit.library.test_guide.test_infrastructure.md)
- [ user_mgmt ](../../test_guide/flow_kit.library.test_guide.user_mgmt.md)

tools

tools

[ codebeamer ](../codebeamer/index.md)

codebeamer

- [ association ](../codebeamer/flow_kit.library.tools.codebeamer.association.md)
- [ attachment ](../codebeamer/flow_kit.library.tools.codebeamer.attachment.md)
- [ background_job ](../codebeamer/flow_kit.library.tools.codebeamer.background_job.md)
- [ baseline ](../codebeamer/flow_kit.library.tools.codebeamer.baseline.md)
- [ branches ](../codebeamer/flow_kit.library.tools.codebeamer.branches.md)
- [ export ](../codebeamer/flow_kit.library.tools.codebeamer.export.md)
- [ group ](../codebeamer/flow_kit.library.tools.codebeamer.group.md)
- [ migration ](../codebeamer/flow_kit.library.tools.codebeamer.migration.md)
- [ project ](../codebeamer/flow_kit.library.tools.codebeamer.project.md)
- [ report ](../codebeamer/flow_kit.library.tools.codebeamer.report.md)
- [ repository_item ](../codebeamer/flow_kit.library.tools.codebeamer.repository_item.md)
- [ role ](../codebeamer/flow_kit.library.tools.codebeamer.role.md)
- [ system ](../codebeamer/flow_kit.library.tools.codebeamer.system.md)
- [ test_management ](../codebeamer/flow_kit.library.tools.codebeamer.test_management.md)
- [ test_run ](../codebeamer/flow_kit.library.tools.codebeamer.test_run.md)
- [ traceability ](../codebeamer/flow_kit.library.tools.codebeamer.traceability.md)
- [ tracker ](../codebeamer/flow_kit.library.tools.codebeamer.tracker.md)
- [ tracker_item ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_item.md)
- [ tracker_item_move ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_item_move.md)
- [ tracker_items_attachment ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_items_attachment.md)
- [ tracker_items_comment ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_items_comment.md)
- [ tracker_permission ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_permission.md)
- [ tracker_report ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_report.md)
- [ tracker_tree ](../codebeamer/flow_kit.library.tools.codebeamer.tracker_tree.md)
- [ user ](../codebeamer/flow_kit.library.tools.codebeamer.user.md)
- [ wiki ](../codebeamer/flow_kit.library.tools.codebeamer.wiki.md)
- [ wikis_comment ](../codebeamer/flow_kit.library.tools.codebeamer.wikis_comment.md)
- [ working_set ](../codebeamer/flow_kit.library.tools.codebeamer.working_set.md)
- [ working_set_permission ](../codebeamer/flow_kit.library.tools.codebeamer.working_set_permission.md)

[ etm ](../etm/index.md)

etm

- [ attachment ](../etm/flow_kit.library.tools.etm.attachment.md)
- [ baseline ](../etm/flow_kit.library.tools.etm.baseline.md)
- [ builddefinition ](../etm/flow_kit.library.tools.etm.builddefinition.md)
- [ buildrecord ](../etm/flow_kit.library.tools.etm.buildrecord.md)
- [ catalog ](../etm/flow_kit.library.tools.etm.catalog.md)
- [ category ](../etm/flow_kit.library.tools.etm.category.md)
- [ categorytype ](../etm/flow_kit.library.tools.etm.categorytype.md)
- [ channel ](../etm/flow_kit.library.tools.etm.channel.md)
- [ component ](../etm/flow_kit.library.tools.etm.component.md)
- [ configuration ](../etm/flow_kit.library.tools.etm.configuration.md)
- [ contributor ](../etm/flow_kit.library.tools.etm.contributor.md)
- [ customattribute ](../etm/flow_kit.library.tools.etm.customattribute.md)
- [ datapool ](../etm/flow_kit.library.tools.etm.datapool.md)
- [ executionelementresult ](../etm/flow_kit.library.tools.etm.executionelementresult.md)
- [ executionresult ](../etm/flow_kit.library.tools.etm.executionresult.md)
- [ executionscript ](../etm/flow_kit.library.tools.etm.executionscript.md)
- [ executionsequence ](../etm/flow_kit.library.tools.etm.executionsequence.md)
- [ executionsequenceresult ](../etm/flow_kit.library.tools.etm.executionsequenceresult.md)
- [ executionvariable ](../etm/flow_kit.library.tools.etm.executionvariable.md)
- [ executionvariablevalue ](../etm/flow_kit.library.tools.etm.executionvariablevalue.md)
- [ executionworkitem ](../etm/flow_kit.library.tools.etm.executionworkitem.md)
- [ job ](../etm/flow_kit.library.tools.etm.job.md)
- [ jobresult ](../etm/flow_kit.library.tools.etm.jobresult.md)
- [ jobscheduler ](../etm/flow_kit.library.tools.etm.jobscheduler.md)
- [ keyword ](../etm/flow_kit.library.tools.etm.keyword.md)
- [ labresource ](../etm/flow_kit.library.tools.etm.labresource.md)
- [ labresourceattribute ](../etm/flow_kit.library.tools.etm.labresourceattribute.md)
- [ objective ](../etm/flow_kit.library.tools.etm.objective.md)
- [ project ](../etm/flow_kit.library.tools.etm.project.md)
- [ remotescript ](../etm/flow_kit.library.tools.etm.remotescript.md)
- [ request ](../etm/flow_kit.library.tools.etm.request.md)
- [ requirement ](../etm/flow_kit.library.tools.etm.requirement.md)
- [ reservation ](../etm/flow_kit.library.tools.etm.reservation.md)
- [ resourcegroup ](../etm/flow_kit.library.tools.etm.resourcegroup.md)
- [ step ](../etm/flow_kit.library.tools.etm.step.md)
- [ stream ](../etm/flow_kit.library.tools.etm.stream.md)
- [ suiteexecutionrecord ](../etm/flow_kit.library.tools.etm.suiteexecutionrecord.md)
- [ tasks ](../etm/flow_kit.library.tools.etm.tasks.md)
- [ teamarea ](../etm/flow_kit.library.tools.etm.teamarea.md)
- [ template ](../etm/flow_kit.library.tools.etm.template.md)
- [ testcase ](../etm/flow_kit.library.tools.etm.testcase.md)
- [ testcell ](../etm/flow_kit.library.tools.etm.testcell.md)
- [ testphase ](../etm/flow_kit.library.tools.etm.testphase.md)
- [ testplan ](../etm/flow_kit.library.tools.etm.testplan.md)
- [ testscript ](../etm/flow_kit.library.tools.etm.testscript.md)
- [ testsuite ](../etm/flow_kit.library.tools.etm.testsuite.md)
- [ testsuitelog ](../etm/flow_kit.library.tools.etm.testsuitelog.md)
- [ tooladapter ](../etm/flow_kit.library.tools.etm.tooladapter.md)
- [ utility ](../etm/flow_kit.library.tools.etm.utility.md)

[ jama ](index.md)

jama

[ abstractitems ](flow_kit.library.tools.jama.abstractitems.md)

[ activities ](flow_kit.library.tools.jama.activities.md)

[ attachments ](flow_kit.library.tools.jama.attachments.md)

[ baselines ](flow_kit.library.tools.jama.baselines.md)

[ categories ](flow_kit.library.tools.jama.categories.md)

[ comments ](flow_kit.library.tools.jama.comments.md)

[ files ](flow_kit.library.tools.jama.files.md)

[ filters ](flow_kit.library.tools.jama.filters.md)

items [ items ](flow_kit.library.tools.jama.items.md)

Table of contents

- [ JamaAddAttachmentToItem ](#jamaaddattachmenttoitem)
- [ JamaAddItem ](#jamaadditem)
- [ JamaAddItemLink ](#jamaadditemlink)
- [ JamaAddTagToItem ](#jamaaddtagtoitem)
- [ JamaApplyCategoriesToAnItem ](#jamaapplycategoriestoanitem)
- [ JamaBreakSyncOnItem ](#jamabreaksynconitem)
- [ JamaConnectItemToGlobalIdPool ](#jamaconnectitemtoglobalidpool)
- [ JamaDeleteItem ](#jamadeleteitem)
- [ JamaDeleteItemLink ](#jamadeleteitemlink)
- [ JamaDuplicateItem ](#jamaduplicateitem)
- [ JamaExecuteTransition ](#jamaexecutetransition)
- [ JamaGetActivities1 ](#jamagetactivities1)
- [ JamaGetAttachments ](#jamagetattachments)
- [ JamaGetBaselinedCategories ](#jamagetbaselinedcategories)
- [ JamaGetCategories ](#jamagetcategories)
- [ JamaGetChildItems ](#jamagetchilditems)
- [ JamaGetDownstreamRelated ](#jamagetdownstreamrelated)
- [ JamaGetDownstreamRelationships ](#jamagetdownstreamrelationships)
- [ JamaGetItem2 ](#jamagetitem2)
- [ JamaGetItems ](#jamagetitems)
- [ JamaGetItemComments1 ](#jamagetitemcomments1)
- [ JamaGetItemLinks ](#jamagetitemlinks)
- [ JamaGetLinkById ](#jamagetlinkbyid)
- [ JamaGetLocation ](#jamagetlocation)
- [ JamaGetLock1 ](#jamagetlock1)
- [ JamaGetParentItem ](#jamagetparentitem)
- [ JamaGetSyncItems ](#jamagetsyncitems)
- [ JamaGetSyncStatus ](#jamagetsyncstatus)
- [ JamaGetTagsOnItem ](#jamagettagsonitem)
- [ JamaGetTagOnItem ](#jamagettagonitem)
- [ JamaGetUpstreamRelated ](#jamagetupstreamrelated)
- [ JamaGetUpstreamRelationships ](#jamagetupstreamrelationships)
- [ JamaGetVersionedCategories ](#jamagetversionedcategories)
- [ JamaGetVersionedItem3 ](#jamagetversioneditem3)
- [ JamaGetVersionsOnItem2 ](#jamagetversionsonitem2)
- [ JamaGetVersionOnItem2 ](#jamagetversiononitem2)
- [ JamaGetWorkflowTransitionOptions ](#jamagetworkflowtransitionoptions)
- [ JamaMoveItems2 ](#jamamoveitems2)
- [ JamaPatchItem ](#jamapatchitem)
- [ JamaPatchItems ](#jamapatchitems)
- [ JamaPutItem ](#jamaputitem)
- [ JamaRemoveAttachmentFromItem ](#jamaremoveattachmentfromitem)
- [ JamaRemoveTagFromItem ](#jamaremovetagfromitem)
- [ JamaUnapplyCategoriesFromAnItem ](#jamaunapplycategoriesfromanitem)
- [ JamaUpdateItemLink ](#jamaupdateitemlink)
- [ JamaUpdateLocation ](#jamaupdatelocation)
- [ JamaUpdateLock1 ](#jamaupdatelock1)

[ itemtypes ](flow_kit.library.tools.jama.itemtypes.md)

[ picklistoptions ](flow_kit.library.tools.jama.picklistoptions.md)

[ picklists ](flow_kit.library.tools.jama.picklists.md)

[ projects ](flow_kit.library.tools.jama.projects.md)

[ relationshiprulesets ](flow_kit.library.tools.jama.relationshiprulesets.md)

[ relationships ](flow_kit.library.tools.jama.relationships.md)

[ relationshiptypes ](flow_kit.library.tools.jama.relationshiptypes.md)

[ releases ](flow_kit.library.tools.jama.releases.md)

[ system ](flow_kit.library.tools.jama.system.md)

[ tags ](flow_kit.library.tools.jama.tags.md)

[ testcycles ](flow_kit.library.tools.jama.testcycles.md)

[ testplans ](flow_kit.library.tools.jama.testplans.md)

[ testruns ](flow_kit.library.tools.jama.testruns.md)

[ usergroups ](flow_kit.library.tools.jama.usergroups.md)

[ users ](flow_kit.library.tools.jama.users.md)

[ jira ](../flow_kit.library.tools.jira.md)

[ llm ](../flow_kit.library.tools.llm.md)

[ polarion ](../polarion/index.md)

polarion

- [ collections ](../polarion/flow_kit.library.tools.polarion.collections.md)
- [ document_attachments ](../polarion/flow_kit.library.tools.polarion.document_attachments.md)
- [ document_comments ](../polarion/flow_kit.library.tools.polarion.document_comments.md)
- [ document_parts ](../polarion/flow_kit.library.tools.polarion.document_parts.md)
- [ documents ](../polarion/flow_kit.library.tools.polarion.documents.md)
- [ enumerations ](../polarion/flow_kit.library.tools.polarion.enumerations.md)
- [ externally_linked_work_items ](../polarion/flow_kit.library.tools.polarion.externally_linked_work_items.md)
- [ feature_selections ](../polarion/flow_kit.library.tools.polarion.feature_selections.md)
- [ icons ](../polarion/flow_kit.library.tools.polarion.icons.md)
- [ jobs ](../polarion/flow_kit.library.tools.polarion.jobs.md)
- [ linked_oslc_resources ](../polarion/flow_kit.library.tools.polarion.linked_oslc_resources.md)
- [ linked_work_items ](../polarion/flow_kit.library.tools.polarion.linked_work_items.md)
- [ page_attachments ](../polarion/flow_kit.library.tools.polarion.page_attachments.md)
- [ pages ](../polarion/flow_kit.library.tools.polarion.pages.md)
- [ plans ](../polarion/flow_kit.library.tools.polarion.plans.md)
- [ project_templates ](../polarion/flow_kit.library.tools.polarion.project_templates.md)
- [ projects ](../polarion/flow_kit.library.tools.polarion.projects.md)
- [ revisions ](../polarion/flow_kit.library.tools.polarion.revisions.md)
- [ roles ](../polarion/flow_kit.library.tools.polarion.roles.md)
- [ test_record_attachments ](../polarion/flow_kit.library.tools.polarion.test_record_attachments.md)
- [ test_records ](../polarion/flow_kit.library.tools.polarion.test_records.md)
- [ test_run_attachments ](../polarion/flow_kit.library.tools.polarion.test_run_attachments.md)
- [ test_run_comments ](../polarion/flow_kit.library.tools.polarion.test_run_comments.md)
- [ test_runs ](../polarion/flow_kit.library.tools.polarion.test_runs.md)
- [ test_step_result_attachments ](../polarion/flow_kit.library.tools.polarion.test_step_result_attachments.md)
- [ test_step_results ](../polarion/flow_kit.library.tools.polarion.test_step_results.md)
- [ test_steps ](../polarion/flow_kit.library.tools.polarion.test_steps.md)
- [ user_groups ](../polarion/flow_kit.library.tools.polarion.user_groups.md)
- [ users ](../polarion/flow_kit.library.tools.polarion.users.md)
- [ work_item_approvals ](../polarion/flow_kit.library.tools.polarion.work_item_approvals.md)
- [ work_item_attachments ](../polarion/flow_kit.library.tools.polarion.work_item_attachments.md)
- [ work_item_comments ](../polarion/flow_kit.library.tools.polarion.work_item_comments.md)
- [ work_item_work_records ](../polarion/flow_kit.library.tools.polarion.work_item_work_records.md)
- [ work_items ](../polarion/flow_kit.library.tools.polarion.work_items.md)

[ trigger ](../../flow_kit.library.trigger.md)

[ user_code ](../../flow_kit.library.user_code.md)

Table of contents

- [ JamaAddAttachmentToItem ](#jamaaddattachmenttoitem)
- [ JamaAddItem ](#jamaadditem)
- [ JamaAddItemLink ](#jamaadditemlink)
- [ JamaAddTagToItem ](#jamaaddtagtoitem)
- [ JamaApplyCategoriesToAnItem ](#jamaapplycategoriestoanitem)
- [ JamaBreakSyncOnItem ](#jamabreaksynconitem)
- [ JamaConnectItemToGlobalIdPool ](#jamaconnectitemtoglobalidpool)
- [ JamaDeleteItem ](#jamadeleteitem)
- [ JamaDeleteItemLink ](#jamadeleteitemlink)
- [ JamaDuplicateItem ](#jamaduplicateitem)
- [ JamaExecuteTransition ](#jamaexecutetransition)
- [ JamaGetActivities1 ](#jamagetactivities1)
- [ JamaGetAttachments ](#jamagetattachments)
- [ JamaGetBaselinedCategories ](#jamagetbaselinedcategories)
- [ JamaGetCategories ](#jamagetcategories)
- [ JamaGetChildItems ](#jamagetchilditems)
- [ JamaGetDownstreamRelated ](#jamagetdownstreamrelated)
- [ JamaGetDownstreamRelationships ](#jamagetdownstreamrelationships)
- [ JamaGetItem2 ](#jamagetitem2)
- [ JamaGetItems ](#jamagetitems)
- [ JamaGetItemComments1 ](#jamagetitemcomments1)
- [ JamaGetItemLinks ](#jamagetitemlinks)
- [ JamaGetLinkById ](#jamagetlinkbyid)
- [ JamaGetLocation ](#jamagetlocation)
- [ JamaGetLock1 ](#jamagetlock1)
- [ JamaGetParentItem ](#jamagetparentitem)
- [ JamaGetSyncItems ](#jamagetsyncitems)
- [ JamaGetSyncStatus ](#jamagetsyncstatus)
- [ JamaGetTagsOnItem ](#jamagettagsonitem)
- [ JamaGetTagOnItem ](#jamagettagonitem)
- [ JamaGetUpstreamRelated ](#jamagetupstreamrelated)
- [ JamaGetUpstreamRelationships ](#jamagetupstreamrelationships)
- [ JamaGetVersionedCategories ](#jamagetversionedcategories)
- [ JamaGetVersionedItem3 ](#jamagetversioneditem3)
- [ JamaGetVersionsOnItem2 ](#jamagetversionsonitem2)
- [ JamaGetVersionOnItem2 ](#jamagetversiononitem2)
- [ JamaGetWorkflowTransitionOptions ](#jamagetworkflowtransitionoptions)
- [ JamaMoveItems2 ](#jamamoveitems2)
- [ JamaPatchItem ](#jamapatchitem)
- [ JamaPatchItems ](#jamapatchitems)
- [ JamaPutItem ](#jamaputitem)
- [ JamaRemoveAttachmentFromItem ](#jamaremoveattachmentfromitem)
- [ JamaRemoveTagFromItem ](#jamaremovetagfromitem)
- [ JamaUnapplyCategoriesFromAnItem ](#jamaunapplycategoriesfromanitem)
- [ JamaUpdateItemLink ](#jamaupdateitemlink)
- [ JamaUpdateLocation ](#jamaupdatelocation)
- [ JamaUpdateLock1 ](#jamaupdatelock1)

# items

Module path: `flow_kit.library.tools.jama.items`

## JamaAddAttachmentToItem

Module path: `flow_kit.library.tools.jama.items.JamaAddAttachmentToItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Add an existing attachment to the item with the specified ID.

**Parameter:**

- JamaAddAttachmentToItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaAddAttachmentToItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaAddAttachmentToItem.**PAR\_\_REQUEST_ITEM_ATTACHMENT**

  - type: `tracetronic_jama_client.models.request_item_attachment.RequestItemAttachment`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaAddAttachmentToItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaAddAttachmentToItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaAddAttachmentToItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaAddAttachmentToItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaAddAttachmentToItem.PAR__REQUEST_ITEM_ATTACHMENT).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_item_attachment.RequestItemAttachment)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaAddItem

Module path: `flow_kit.library.tools.jama.items.JamaAddItem`

> Create a new item.

**Parameter:**

- JamaAddItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaAddItem.**PAR\_\_REQUEST_ITEM**

  - type: `tracetronic_jama_client.models.request_item.RequestItem`
  - (required)

- JamaAddItem.**PAR\_\_SET_GLOBAL_ID_MANUALLY**

  - type: `bool`

  - optional, default = `None`

  - This value must be set to true if you attempt to manually set the Global ID field of an item

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaAddItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaAddItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaAddItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaAddItem.PAR__REQUEST_ITEM).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_item.RequestItem)
           # optional parameter: Assign(JamaAddItem.PAR__SET_GLOBAL_ID_MANUALLY).to_,  # TODO: assign parameter if needed (type: bool)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaAddItemLink

Module path: `flow_kit.library.tools.jama.items.JamaAddItemLink`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Create a new link for the item with the specified ID.

**Parameter:**

- JamaAddItemLink.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaAddItemLink.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaAddItemLink.**PAR\_\_REQUEST_LINK**

  - type: `tracetronic_jama_client.models.request_link.RequestLink`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaAddItemLink  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaAddItemLink().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaAddItemLink.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaAddItemLink.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaAddItemLink.PAR__REQUEST_LINK).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_link.RequestLink)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaAddTagToItem

Module path: `flow_kit.library.tools.jama.items.JamaAddTagToItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Add an existing tag to the item with the specified ID.

**Parameter:**

- JamaAddTagToItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaAddTagToItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaAddTagToItem.**PAR\_\_REQUEST_ITEM_TAG**

  - type: `tracetronic_jama_client.models.request_item_tag.RequestItemTag`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaAddTagToItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaAddTagToItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaAddTagToItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaAddTagToItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaAddTagToItem.PAR__REQUEST_ITEM_TAG).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_item_tag.RequestItemTag)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaApplyCategoriesToAnItem

Module path: `flow_kit.library.tools.jama.items.JamaApplyCategoriesToAnItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Apply categories to an item.

**Parameter:**

- JamaApplyCategoriesToAnItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaApplyCategoriesToAnItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaApplyCategoriesToAnItem.**PAR\_\_REQUEST_CATEGORY_PATHS**

  - type: `tracetronic_jama_client.models.request_category_paths.RequestCategoryPaths`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaApplyCategoriesToAnItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaApplyCategoriesToAnItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaApplyCategoriesToAnItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaApplyCategoriesToAnItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaApplyCategoriesToAnItem.PAR__REQUEST_CATEGORY_PATHS).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_category_paths.RequestCategoryPaths)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaBreakSyncOnItem

Module path: `flow_kit.library.tools.jama.items.JamaBreakSyncOnItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Remove an existing item from the Global ID pool of the item with the specified ID (break sync).

**Parameter:**

- JamaBreakSyncOnItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaBreakSyncOnItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaBreakSyncOnItem.**PAR\_\_SYNCED_ITEM_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaBreakSyncOnItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaBreakSyncOnItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaBreakSyncOnItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaBreakSyncOnItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaBreakSyncOnItem.PAR__SYNCED_ITEM_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaConnectItemToGlobalIdPool

Module path: `flow_kit.library.tools.jama.items.JamaConnectItemToGlobalIdPool`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Add an existing item to the Global ID pool of the item with the specified ID.

**Parameter:**

- JamaConnectItemToGlobalIdPool.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaConnectItemToGlobalIdPool.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaConnectItemToGlobalIdPool.**PAR\_\_REQUEST_ITEM_SYNCED_ITEM**

  - type: `tracetronic_jama_client.models.request_item_synced_item.RequestItemSyncedItem`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaConnectItemToGlobalIdPool  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaConnectItemToGlobalIdPool().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaConnectItemToGlobalIdPool.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaConnectItemToGlobalIdPool.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaConnectItemToGlobalIdPool.PAR__REQUEST_ITEM_SYNCED_ITEM).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_item_synced_item.RequestItemSyncedItem)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaDeleteItem

Module path: `flow_kit.library.tools.jama.items.JamaDeleteItem`

> Delete the item with the specified ID (item becomes inactive and can be un-deleted if necessary).

**Parameter:**

- JamaDeleteItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaDeleteItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaDeleteItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaDeleteItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaDeleteItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaDeleteItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaDeleteItemLink

Module path: `flow_kit.library.tools.jama.items.JamaDeleteItemLink`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Delete the link with the specified ID.

**Parameter:**

- JamaDeleteItemLink.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaDeleteItemLink.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaDeleteItemLink.**PAR\_\_LINK_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaDeleteItemLink  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaDeleteItemLink().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaDeleteItemLink.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaDeleteItemLink.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaDeleteItemLink.PAR__LINK_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaDuplicateItem

Module path: `flow_kit.library.tools.jama.items.JamaDuplicateItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Create a duplicate of item.

**Parameter:**

- JamaDuplicateItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaDuplicateItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaDuplicateItem.**PAR\_\_DUPLICATE_CONFIG**

  - type: `tracetronic_jama_client.models.duplicate_config.DuplicateConfig`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `int`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaDuplicateItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaDuplicateItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaDuplicateItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaDuplicateItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaDuplicateItem.PAR__DUPLICATE_CONFIG).to_,  # TODO: assign parameter if needed (type: tracetronic_jama_client.models.duplicate_config.DuplicateConfig)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type int
        )

------------------------------------------------------------------------

## JamaExecuteTransition

Module path: `flow_kit.library.tools.jama.items.JamaExecuteTransition`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Executes a workflow transition for the item with the specified ID. Valid transitions can be found at /items/{id}/workflowtransitionoptions.

**Parameter:**

- JamaExecuteTransition.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaExecuteTransition.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaExecuteTransition.**PAR\_\_REQUEST_TRANSITION**

  - type: `tracetronic_jama_client.models.request_transition.RequestTransition`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaExecuteTransition  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaExecuteTransition().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaExecuteTransition.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaExecuteTransition.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaExecuteTransition.PAR__REQUEST_TRANSITION).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_transition.RequestTransition)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaGetActivities1

Module path: `flow_kit.library.tools.jama.items.JamaGetActivities1`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all activities for the item with the specified ID.

**Parameter:**

- JamaGetActivities1.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetActivities1.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetActivities1.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetActivities1.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.activity.Activity]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetActivities1  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetActivities1().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetActivities1.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetActivities1.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetActivities1.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetActivities1.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.activity.Activity]
        )

------------------------------------------------------------------------

## JamaGetAttachments

Module path: `flow_kit.library.tools.jama.items.JamaGetAttachments`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all attachments for the item with the specified ID.

**Parameter:**

- JamaGetAttachments.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetAttachments.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetAttachments.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetAttachments.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.attachment.Attachment]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetAttachments  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetAttachments().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetAttachments.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetAttachments.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetAttachments.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetAttachments.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.attachment.Attachment]
        )

------------------------------------------------------------------------

## JamaGetBaselinedCategories

Module path: `flow_kit.library.tools.jama.items.JamaGetBaselinedCategories`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all categories for the item associated with a specific baseline.

**Parameter:**

- JamaGetBaselinedCategories.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetBaselinedCategories.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetBaselinedCategories.**PAR\_\_BASELINE_ID**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.category_path_info.CategoryPathInfo]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetBaselinedCategories  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetBaselinedCategories().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetBaselinedCategories.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetBaselinedCategories.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetBaselinedCategories.PAR__BASELINE_ID).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.category_path_info.CategoryPathInfo]
        )

------------------------------------------------------------------------

## JamaGetCategories

Module path: `flow_kit.library.tools.jama.items.JamaGetCategories`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Retrieve all categories for an item.

**Parameter:**

- JamaGetCategories.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetCategories.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetCategories.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetCategories.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.category_path_info.CategoryPathInfo]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetCategories  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetCategories().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetCategories.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetCategories.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetCategories.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetCategories.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.category_path_info.CategoryPathInfo]
        )

------------------------------------------------------------------------

## JamaGetChildItems

Module path: `flow_kit.library.tools.jama.items.JamaGetChildItems`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all children items for the item with the specified ID.

**Parameter:**

- JamaGetChildItems.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetChildItems.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetChildItems.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetChildItems.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.item.Item]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetChildItems  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetChildItems().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetChildItems.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetChildItems.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetChildItems.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetChildItems.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.item.Item]
        )

------------------------------------------------------------------------

## JamaGetDownstreamRelated

Module path: `flow_kit.library.tools.jama.items.JamaGetDownstreamRelated`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all downstream related items for the item with the specified ID.

**Parameter:**

- JamaGetDownstreamRelated.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetDownstreamRelated.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetDownstreamRelated.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetDownstreamRelated.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.item.Item]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetDownstreamRelated  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetDownstreamRelated().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetDownstreamRelated.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetDownstreamRelated.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetDownstreamRelated.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetDownstreamRelated.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.item.Item]
        )

------------------------------------------------------------------------

## JamaGetDownstreamRelationships

Module path: `flow_kit.library.tools.jama.items.JamaGetDownstreamRelationships`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all downstream relationships for the item with the specified ID.

**Parameter:**

- JamaGetDownstreamRelationships.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetDownstreamRelationships.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetDownstreamRelationships.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetDownstreamRelationships.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.relationship.Relationship]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetDownstreamRelationships  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetDownstreamRelationships().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetDownstreamRelationships.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetDownstreamRelationships.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetDownstreamRelationships.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetDownstreamRelationships.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.relationship.Relationship]
        )

------------------------------------------------------------------------

## JamaGetItem2

Module path: `flow_kit.library.tools.jama.items.JamaGetItem2`

> Get the item with the specified ID.

**Parameter:**

- JamaGetItem2.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetItem2.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.item.Item`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetItem2  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetItem2().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetItem2.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetItem2.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.item.Item
        )

------------------------------------------------------------------------

## JamaGetItems

Module path: `flow_kit.library.tools.jama.items.JamaGetItems`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all items in the project with the specified ID.

**Parameter:**

- JamaGetItems.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetItems.**PAR\_\_PROJECT**

  - type: `int`
  - (required)

- JamaGetItems.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetItems.**PAR\_\_ROOT_ONLY**

  - type: `bool`

  - optional, default = `None`

  - Set this to true to only get root-level nodes from the item tree

- JamaGetItems.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.item.Item]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetItems  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetItems().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetItems.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetItems.PAR__PROJECT).to_,  # TODO: assign parameter (type: int)
           # optional parameter: Assign(JamaGetItems.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetItems.PAR__ROOT_ONLY).to_,  # TODO: assign parameter if needed (type: bool)
           # optional parameter: Assign(JamaGetItems.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.item.Item]
        )

------------------------------------------------------------------------

## JamaGetItemComments1

Module path: `flow_kit.library.tools.jama.items.JamaGetItemComments1`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all comments for the item with the specified ID.

**Parameter:**

- JamaGetItemComments1.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetItemComments1.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetItemComments1.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetItemComments1.**PAR\_\_ROOT_COMMENTS_ONLY**

  - type: `bool`

  - optional, default = `None`

  - 

- JamaGetItemComments1.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.comment.Comment]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetItemComments1  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetItemComments1().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetItemComments1.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetItemComments1.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetItemComments1.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetItemComments1.PAR__ROOT_COMMENTS_ONLY).to_,  # TODO: assign parameter if needed (type: bool)
           # optional parameter: Assign(JamaGetItemComments1.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.comment.Comment]
        )

------------------------------------------------------------------------

## JamaGetItemLinks

Module path: `flow_kit.library.tools.jama.items.JamaGetItemLinks`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all links for the item with the specified ID.

**Parameter:**

- JamaGetItemLinks.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetItemLinks.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetItemLinks.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetItemLinks.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.link.Link]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetItemLinks  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetItemLinks().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetItemLinks.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetItemLinks.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetItemLinks.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetItemLinks.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.link.Link]
        )

------------------------------------------------------------------------

## JamaGetLinkById

Module path: `flow_kit.library.tools.jama.items.JamaGetLinkById`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the link with the specified ID.

**Parameter:**

- JamaGetLinkById.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetLinkById.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetLinkById.**PAR\_\_LINK_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.link.Link`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetLinkById  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetLinkById().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetLinkById.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetLinkById.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetLinkById.PAR__LINK_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.link.Link
        )

------------------------------------------------------------------------

## JamaGetLocation

Module path: `flow_kit.library.tools.jama.items.JamaGetLocation`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the location for the item with the specified ID.

**Parameter:**

- JamaGetLocation.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetLocation.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.location.Location`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetLocation  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetLocation().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetLocation.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetLocation.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.location.Location
        )

------------------------------------------------------------------------

## JamaGetLock1

Module path: `flow_kit.library.tools.jama.items.JamaGetLock1`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the locked state, last locked date, and last locked by user for the item with the specified ID.

**Parameter:**

- JamaGetLock1.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetLock1.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.lock.Lock`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetLock1  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetLock1().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetLock1.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetLock1.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.lock.Lock
        )

------------------------------------------------------------------------

## JamaGetParentItem

Module path: `flow_kit.library.tools.jama.items.JamaGetParentItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the parent item for the item with the specified ID.

**Parameter:**

- JamaGetParentItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetParentItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.item.Item`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetParentItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetParentItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetParentItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetParentItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.item.Item
        )

------------------------------------------------------------------------

## JamaGetSyncItems

Module path: `flow_kit.library.tools.jama.items.JamaGetSyncItems`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all synchronized items for the item with the specified ID.

**Parameter:**

- JamaGetSyncItems.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetSyncItems.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetSyncItems.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetSyncItems.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.item.Item]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetSyncItems  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetSyncItems().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetSyncItems.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetSyncItems.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetSyncItems.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetSyncItems.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.item.Item]
        )

------------------------------------------------------------------------

## JamaGetSyncStatus

Module path: `flow_kit.library.tools.jama.items.JamaGetSyncStatus`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the sync status for the synced item with the specified ID.

**Parameter:**

- JamaGetSyncStatus.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetSyncStatus.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetSyncStatus.**PAR\_\_SYNCED_ITEM_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.sync_status.SyncStatus`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetSyncStatus  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetSyncStatus().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetSyncStatus.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetSyncStatus.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetSyncStatus.PAR__SYNCED_ITEM_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.sync_status.SyncStatus
        )

------------------------------------------------------------------------

## JamaGetTagsOnItem

Module path: `flow_kit.library.tools.jama.items.JamaGetTagsOnItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all tags for the item with the specified ID.

**Parameter:**

- JamaGetTagsOnItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetTagsOnItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetTagsOnItem.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetTagsOnItem.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.tag.Tag]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetTagsOnItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetTagsOnItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetTagsOnItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetTagsOnItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetTagsOnItem.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetTagsOnItem.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.tag.Tag]
        )

------------------------------------------------------------------------

## JamaGetTagOnItem

Module path: `flow_kit.library.tools.jama.items.JamaGetTagOnItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the tag with the specified ID.

**Parameter:**

- JamaGetTagOnItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetTagOnItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetTagOnItem.**PAR\_\_TAG_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.tag.Tag`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetTagOnItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetTagOnItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetTagOnItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetTagOnItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetTagOnItem.PAR__TAG_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.tag.Tag
        )

------------------------------------------------------------------------

## JamaGetUpstreamRelated

Module path: `flow_kit.library.tools.jama.items.JamaGetUpstreamRelated`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all upstream related items for the item with the specified ID.

**Parameter:**

- JamaGetUpstreamRelated.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetUpstreamRelated.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetUpstreamRelated.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetUpstreamRelated.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.item.Item]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetUpstreamRelated  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetUpstreamRelated().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetUpstreamRelated.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetUpstreamRelated.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetUpstreamRelated.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetUpstreamRelated.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.item.Item]
        )

------------------------------------------------------------------------

## JamaGetUpstreamRelationships

Module path: `flow_kit.library.tools.jama.items.JamaGetUpstreamRelationships`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all upstream relationships for the item with the specified ID.

**Parameter:**

- JamaGetUpstreamRelationships.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetUpstreamRelationships.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetUpstreamRelationships.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetUpstreamRelationships.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.relationship.Relationship]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetUpstreamRelationships  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetUpstreamRelationships().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetUpstreamRelationships.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetUpstreamRelationships.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetUpstreamRelationships.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetUpstreamRelationships.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.relationship.Relationship]
        )

------------------------------------------------------------------------

## JamaGetVersionedCategories

Module path: `flow_kit.library.tools.jama.items.JamaGetVersionedCategories`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all versioned categories for the item with the specified version number.

**Parameter:**

- JamaGetVersionedCategories.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetVersionedCategories.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetVersionedCategories.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetVersionedCategories.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

- JamaGetVersionedCategories.**PAR\_\_VERSION_NUMBER**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.abstract_item.AbstractItem]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetVersionedCategories  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetVersionedCategories().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetVersionedCategories.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetVersionedCategories.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetVersionedCategories.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetVersionedCategories.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetVersionedCategories.PAR__VERSION_NUMBER).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.abstract_item.AbstractItem]
        )

------------------------------------------------------------------------

## JamaGetVersionedItem3

Module path: `flow_kit.library.tools.jama.items.JamaGetVersionedItem3`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the snapshot of the item at the specified version.

**Parameter:**

- JamaGetVersionedItem3.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetVersionedItem3.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetVersionedItem3.**PAR\_\_VERSION_NUM**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.versioned_attachment.VersionedAttachment`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetVersionedItem3  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetVersionedItem3().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetVersionedItem3.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetVersionedItem3.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetVersionedItem3.PAR__VERSION_NUM).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.versioned_attachment.VersionedAttachment
        )

------------------------------------------------------------------------

## JamaGetVersionsOnItem2

Module path: `flow_kit.library.tools.jama.items.JamaGetVersionsOnItem2`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all versions for the item with the specified ID.

**Parameter:**

- JamaGetVersionsOnItem2.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetVersionsOnItem2.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetVersionsOnItem2.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetVersionsOnItem2.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.version.Version]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetVersionsOnItem2  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetVersionsOnItem2().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetVersionsOnItem2.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetVersionsOnItem2.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetVersionsOnItem2.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetVersionsOnItem2.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.version.Version]
        )

------------------------------------------------------------------------

## JamaGetVersionOnItem2

Module path: `flow_kit.library.tools.jama.items.JamaGetVersionOnItem2`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get the numbered version for the item with the specified ID.

**Parameter:**

- JamaGetVersionOnItem2.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetVersionOnItem2.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetVersionOnItem2.**PAR\_\_VERSION_NUM**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.version.Version`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetVersionOnItem2  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetVersionOnItem2().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetVersionOnItem2.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetVersionOnItem2.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaGetVersionOnItem2.PAR__VERSION_NUM).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.version.Version
        )

------------------------------------------------------------------------

## JamaGetWorkflowTransitionOptions

Module path: `flow_kit.library.tools.jama.items.JamaGetWorkflowTransitionOptions`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Get all valid workflow transitions that can be made on the item with the specified ID.

**Parameter:**

- JamaGetWorkflowTransitionOptions.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaGetWorkflowTransitionOptions.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaGetWorkflowTransitionOptions.**PAR\_\_MAX_RESULTS**

  - type: `int`

  - optional, default = `None`

  - The amount of results to be queried. If not set, all are queried.

- JamaGetWorkflowTransitionOptions.**PAR\_\_START_AT**

  - type: `int`

  - optional, default = `None`

  - 

**Returns:**

> Returns the result object.

**Return type:**

> `list[tracetronic_jama_client.models.workflow_transition.WorkflowTransition]`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaGetWorkflowTransitionOptions  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaGetWorkflowTransitionOptions().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaGetWorkflowTransitionOptions.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaGetWorkflowTransitionOptions.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # optional parameter: Assign(JamaGetWorkflowTransitionOptions.PAR__MAX_RESULTS).to_,  # TODO: assign parameter if needed (type: int)
           # optional parameter: Assign(JamaGetWorkflowTransitionOptions.PAR__START_AT).to_,  # TODO: assign parameter if needed (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type list[tracetronic_jama_client.models.workflow_transition.WorkflowTransition]
        )

------------------------------------------------------------------------

## JamaMoveItems2

Module path: `flow_kit.library.tools.jama.items.JamaMoveItems2`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> move_items2.

**Parameter:**

- JamaMoveItems2.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaMoveItems2.**PAR\_\_TREE_LOCATION_REQUEST**

  - type: `tracetronic_jama_client.models.tree_location_request.TreeLocationRequest`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaMoveItems2  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaMoveItems2().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaMoveItems2.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaMoveItems2.PAR__TREE_LOCATION_REQUEST).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.tree_location_request.TreeLocationRequest)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaPatchItem

Module path: `flow_kit.library.tools.jama.items.JamaPatchItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the item with the specified ID.

**Parameter:**

- JamaPatchItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaPatchItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaPatchItem.**PAR\_\_REQUEST_PATCH_OPERATION**

  - type: `list[tracetronic_jama_client.models.request_patch_operation.RequestPatchOperation]`
  - (required)

- JamaPatchItem.**PAR\_\_RETURN_ITEM**

  - type: `bool`

  - optional, default = `None`

  - 

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaPatchItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaPatchItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaPatchItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaPatchItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaPatchItem.PAR__REQUEST_PATCH_OPERATION).to_,  # TODO: assign parameter (type: list[tracetronic_jama_client.models.request_patch_operation.RequestPatchOperation])
           # optional parameter: Assign(JamaPatchItem.PAR__RETURN_ITEM).to_,  # TODO: assign parameter if needed (type: bool)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaPatchItems

Module path: `flow_kit.library.tools.jama.items.JamaPatchItems`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the items with the specified ID as an asynchronous request (a successful response signifies that the work was started and a work identifier is given. This identifier will be used in a future feature).

**Parameter:**

- JamaPatchItems.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaPatchItems.**PAR\_\_REQUEST_ITEMS_PATCH_OPERATIONS**

  - type: `list[tracetronic_jama_client.models.request_items_patch_operations.RequestItemsPatchOperations]`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `tracetronic_jama_client.models.documents_patch_result.DocumentsPatchResult`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaPatchItems  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaPatchItems().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaPatchItems.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaPatchItems.PAR__REQUEST_ITEMS_PATCH_OPERATIONS).to_,  # TODO: assign parameter (type: list[tracetronic_jama_client.models.request_items_patch_operations.RequestItemsPatchOperations])
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type tracetronic_jama_client.models.documents_patch_result.DocumentsPatchResult
        )

------------------------------------------------------------------------

## JamaPutItem

Module path: `flow_kit.library.tools.jama.items.JamaPutItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the item with the specified ID.

**Parameter:**

- JamaPutItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaPutItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaPutItem.**PAR\_\_REQUEST_ITEM**

  - type: `tracetronic_jama_client.models.request_item.RequestItem`
  - (required)

- JamaPutItem.**PAR\_\_SET_GLOBAL_ID_MANUALLY**

  - type: `bool`

  - optional, default = `None`

  - This value must be set to true if you attempt to manually set the Global ID field of an item

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaPutItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaPutItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaPutItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaPutItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaPutItem.PAR__REQUEST_ITEM).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_item.RequestItem)
           # optional parameter: Assign(JamaPutItem.PAR__SET_GLOBAL_ID_MANUALLY).to_,  # TODO: assign parameter if needed (type: bool)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaRemoveAttachmentFromItem

Module path: `flow_kit.library.tools.jama.items.JamaRemoveAttachmentFromItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Remove an existing attachment from the item.

**Parameter:**

- JamaRemoveAttachmentFromItem.**PAR\_\_ATTACHMENT_ID**

  - type: `int`
  - (required)

- JamaRemoveAttachmentFromItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaRemoveAttachmentFromItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

**Returns:**

> Returns the result object.

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaRemoveAttachmentFromItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaRemoveAttachmentFromItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaRemoveAttachmentFromItem.PAR__ATTACHMENT_ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaRemoveAttachmentFromItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaRemoveAttachmentFromItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaRemoveTagFromItem

Module path: `flow_kit.library.tools.jama.items.JamaRemoveTagFromItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Remove an existing tag from the item with the specified ID.

**Parameter:**

- JamaRemoveTagFromItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaRemoveTagFromItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaRemoveTagFromItem.**PAR\_\_TAG_ID**

  - type: `int`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaRemoveTagFromItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaRemoveTagFromItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaRemoveTagFromItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaRemoveTagFromItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaRemoveTagFromItem.PAR__TAG_ID).to_,  # TODO: assign parameter (type: int)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaUnapplyCategoriesFromAnItem

Module path: `flow_kit.library.tools.jama.items.JamaUnapplyCategoriesFromAnItem`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Unapply categories from an item.

**Parameter:**

- JamaUnapplyCategoriesFromAnItem.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaUnapplyCategoriesFromAnItem.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaUnapplyCategoriesFromAnItem.**PAR\_\_REQUEST_CATEGORY_PATHS**

  - type: `tracetronic_jama_client.models.request_category_paths.RequestCategoryPaths`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaUnapplyCategoriesFromAnItem  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaUnapplyCategoriesFromAnItem().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaUnapplyCategoriesFromAnItem.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaUnapplyCategoriesFromAnItem.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaUnapplyCategoriesFromAnItem.PAR__REQUEST_CATEGORY_PATHS).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_category_paths.RequestCategoryPaths)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaUpdateItemLink

Module path: `flow_kit.library.tools.jama.items.JamaUpdateItemLink`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the link with the specified ID.

**Parameter:**

- JamaUpdateItemLink.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaUpdateItemLink.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaUpdateItemLink.**PAR\_\_LINK_ID**

  - type: `int`
  - (required)

- JamaUpdateItemLink.**PAR\_\_REQUEST_LINK**

  - type: `tracetronic_jama_client.models.request_link.RequestLink`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaUpdateItemLink  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaUpdateItemLink().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaUpdateItemLink.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaUpdateItemLink.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaUpdateItemLink.PAR__LINK_ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaUpdateItemLink.PAR__REQUEST_LINK).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_link.RequestLink)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

## JamaUpdateLocation

Module path: `flow_kit.library.tools.jama.items.JamaUpdateLocation`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the location for the item with the specified ID as an asynchronous request (a successful response signifies that the work was started and a work identifier is given. This identifier will be used in a future feature). Any child items are moved along with this item. Note that this currently only supports moving items between projects.

**Parameter:**

- JamaUpdateLocation.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaUpdateLocation.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaUpdateLocation.**PAR\_\_REQUEST_MOVE_LOCATION**

  - type: `tracetronic_jama_client.models.request_move_location.RequestMoveLocation`
  - (required)

**Returns:**

> Returns the result object.

**Return type:**

> `str`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaUpdateLocation  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaUpdateLocation().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaUpdateLocation.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaUpdateLocation.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaUpdateLocation.PAR__REQUEST_MOVE_LOCATION).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_move_location.RequestMoveLocation)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type str
        )

------------------------------------------------------------------------

## JamaUpdateLock1

Module path: `flow_kit.library.tools.jama.items.JamaUpdateLock1`

EXPERIMENTAL

This block is experimental. This means that the block has not been fully tested and may therefore exhibit unexpected behavior.

> Update the locked state of the item with the specified ID.

**Parameter:**

- JamaUpdateLock1.**PAR\_\_ID**

  - type: `int`
  - (required)

- JamaUpdateLock1.**PAR\_\_JAMA_CONFIG**

  - type: `flow_kit.tools.jama.jama.Jama`
  - Created by block JamaInit.

- JamaUpdateLock1.**PAR\_\_REQUEST_LOCK**

  - type: `tracetronic_jama_client.models.request_lock.RequestLock`
  - (required)

**Return type:**

> `None`

.add_block_with(...) snippet

        # from flow_kit.library.tools.jama.items import JamaUpdateLock1  # TODO: move import to top of this file if needed
        .add_block_with(
            block_name := JamaUpdateLock1().with_label('Your label here'),  # TODO: change block_name  # TODO: set label 
            Assign(JamaUpdateLock1.PAR__ID).to_,  # TODO: assign parameter (type: int)
           Assign(JamaUpdateLock1.PAR__JAMA_CONFIG).to_,  # TODO: assign parameter (type: flow_kit.tools.jama.jama.Jama)
           Assign(JamaUpdateLock1.PAR__REQUEST_LOCK).to_,  # TODO: assign parameter (type: tracetronic_jama_client.models.request_lock.RequestLock)
           # result_alias='alias_name',  # TODO: uncomment and change alias_name if needed
            # required_blocks=[],  # TODO: uncomment and add required blocks here if needed
            # block returns type None
        )

------------------------------------------------------------------------

![](data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHZpZXdib3g9IjAgMCAyNCAyNCI+PHBhdGggZD0iTTEzIDIwaC0yVjhsLTUuNSA1LjUtMS40Mi0xLjQyTDEyIDQuMTZsNy45MiA3LjkyLTEuNDIgMS40MkwxMyA4eiIgLz48L3N2Zz4=) Back to top
